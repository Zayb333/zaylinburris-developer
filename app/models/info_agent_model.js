export class Info_Agent {
    constructor(greeting, message, resumeLink, contactLink) {
        this.greeting = greeting;
        this.message = message;
        this.resumeLink = resumeLink;
        this.contactLink = contactLink;
    }
}